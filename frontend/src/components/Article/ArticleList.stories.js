import ArticleList from './ArticleList';

export default {
  title: 'Component/ArticleList',
  component: ArticleList,
};

const Template = (args) => <ArticleList />;

export const Basic = Template.bind({});

Basic.args = {};
