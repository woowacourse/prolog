const PATH = {
  ROOT: '/',
  MYPAGE: '/:username',
  MYPAGE_POSTS: '/:username/posts',
  MYPAGE_ACCOUNT: '/:username/account',
  LOGIN_CALLBACK: '/login/callback',
  POST: '/posts',
  NEW_POST: '/post/write',
};

export default PATH;
