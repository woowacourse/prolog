server:
  port: 5000
spring:
  flyway:
    enabled: false
management:
  health:
    elasticsearch:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "prometheus"
  metrics:
    tags:
      application: prolog
